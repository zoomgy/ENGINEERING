#include <stdio.h>
#include <string.h>

int main(){
    char a[200];
    printf("\nEnter the string : ");
    fgets (a,50,stdin);
        int x=strlen(a);
        for (int i=0;i<x;i++){

        }
    return 0;
}